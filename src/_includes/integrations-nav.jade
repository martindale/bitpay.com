- var menu = { 'Integrated': '#integrated', 'Plugins': '#plugins', 'Point of Sale': '#pos', 'Billing': '#billing', 'Donations': '#donations' };

.border-bottom-gray
  .row
    .column.medium-12
      ul.inline-list.subnav
        each val, key in menu
          li
            if integrationSelected === key
              a.subnav-link.uppercase.selected(href=val, title=key)= key
            else
              a.subnav-link.uppercase(href=val, title=key)= key
